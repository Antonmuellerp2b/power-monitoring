# Rename this file to .env before using it
# Will be ignored by git afterwards due to .gitignore

# --- User and Group IDs ---
MY_UID=0
MY_GID=0

# --- InfluxDB ---
INFLUXDB_PORT=8086
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=adminpass
INFLUXDB_ORG=my-org
INFLUXDB_BUCKET=pac2200
INFLUXDB_TOKEN=my-token
INFLUXDB_URL=http://influxdb

# --- Cloud InfluxDB ---
CLOUD_INFLUX_HOST=<cloud-ip>
CLOUD_INFLUX_PORT=8086
CLOUD_INFLUX_TOKEN=cloud-token
CLOUD_INFLUX_ORG=my-org
CLOUD_INFLUX_BUCKET=cloud-pac2200
CLOUD_INFLUX_PROTOCOL=https

# --- Grafana ---
GRAFANA_PORT=4300
GRAFANA_PASSWORD=adminpass

# --- PAC2200 Monitoring ---

# Meter type and url - choose the correct one for your meter and uncomment it, comment out the other one

# METER_TYPE=pac2200                                      # For PAC2200
# METER_URL=http://<local-meter-ip>/data.json?type=       # For PAC2200
# METER_TYPE=janitza                                      # For Janitza
# METER_URL=<local-meter-ip>                              # For Janitza 

POLL_INTERVAL_INST=5s
SITE_ID=default_site
CLOUD_FLUSH_INTERVAL=10     # determines how often data is send to the cloud (interval in seconds)

# --- E-Mail alerts ---
# Set GF_SMTP_ENABLED=true if e-mail user/password is set!
GF_SMTP_ENABLED=
# GF_SMTP_USER=
# GF_SMTP_PASSWORD=

# Alert email recipient (use as many e-mails as needed, comma-separated)
ALERT_EMAIL_RECIPIENT=change_this@gmail.com,change_this_aswell@gmail.com

# --- Alert Thresholds ---
# Power imbalance alert
PHASE_IMBALANCE_RATIO_THRESHOLD=1.5
PHASE_IMBALANCE_MIN_KW=1
DATASOURCE_UID=P951FEA4DE68E13C5

# Power sum max alert
TOTAL_POWER_THRESHOLD_KW=100